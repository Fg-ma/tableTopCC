# dirs
mongo_base_dir: /mnt/mongo
vault_dir: /mnt/vault
local_ca_path: "/home/n1/Desktop/tableTopSecrets/ca.pem"

# Users / Groups
mongo_certs_user: mongocerts
mongo_certs_group: mongocerts
mongo_vault_agent_user: mongovaultagent
mongo_vault_agent_group: mongovaultagent
vault_agent_user: vaultagent
vault_agent_group: vaultagent
vault_agent_navigational_group: navigationalvaultagent
vault_agent_authorities_group: authoritiesvaultagent
vault_agent_bin_group: binvaultagent

# Vault
vault_ip: 10.5.0.100
hashi_vault_port: 8200
mongo_admin_secret_path: "kv/data/mongo/admin"
ca_key_secret_path: "kv/data/tableTop/ca/ca.key"
vault_url: "https://10.5.0.100:8200"

# Vault server
tabletop_vault_server_ip: 10.5.0.100
tabletop_vault_server_port: 2223

# Hosts
config_server_hosts:
  - 10.60.0.101:27019
  - 10.60.0.102:27019
  - 10.60.0.103:27019

# Primaries
primary_config_node: 10.60.0.101
primary_shard_node: 10.50.0.101
mongos_node: 10.40.0.101

# Ports
config_port: 27019
shard_port: 27020
mongos_port: 27021

# Certs
cert_country: US
cert_state: FL
cert_locality: CNTL
cert_organization: TableTop
config_cert_ou: MongoInternalCluster
shard_cert_ou: MongoInternalCluster
mongos_cert_ou: MongosInternalCluster
mongos_client_cert_ou: MongosClient

# Subnets
admin_subnet: 5
mongo_mongos_subnet: 40
mongo_shard_subnet: 50
mongo_config_subnet: 60

# Replica sets
config_repl_set_name: cfgRS1
shard_repl_set_name: shardRS1

# Cluster roles
config_cluster_role: configsvr
shard_cluster_role: shardsvr
